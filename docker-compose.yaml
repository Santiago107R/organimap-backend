
services: 
  db: 
    image: postgres:17
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    container_name: organimap
    volumes:
      - ./postgres:/var/lib/postgresql/data